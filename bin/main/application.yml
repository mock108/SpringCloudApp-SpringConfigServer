server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/mock108/springcloudapp-configserver.git
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/